"""Validation cases and error metrics shared by the tests and the benchmark harness.

Lives outside src/ on purpose. The simulation never imports it, so it does
not belong in the dependency matrix that scripts/gen_system_map.py draws
from src/.
"""
